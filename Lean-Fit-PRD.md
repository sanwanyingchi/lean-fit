# Lean Fit 产品需求文档

版本：MVP 1.0  
平台：iOS  
产品形态：本地优先的个人力量训练记录 App

## 1. 产品定义

Lean Fit 面向每周训练 2–5 次的普通力量训练者，帮助用户快速开始训练、参考上一次表现完成每组记录，并通过真实训练数据判断是否持续进步。产品围绕“开始训练—记录重量与次数—完成训练—反馈真实进步—自动打卡—回看趋势”建立稳定闭环。

产品不负责生成复杂训练计划，不以“处方”“课程”等方式表达训练建议。MVP 不包含社交、排行榜、课程商城、饮食记录、自动生成完整训练计划、金币经验体系、Apple Watch 独立应用、HealthKit 自动同步、云端账号、多设备同步或 AI 医疗建议。

## 2. 用户与目标

目标用户为 18 岁以上的普通力量训练者，通常以胸、背、腿、肩训练为主，需要记录重量、组数和次数，但不希望使用复杂专业工具。

| 目标 | 成功标准 |
|---|---|
| 快速开始训练 | 点击中心加号后，30 秒内选四个动作并开始 |
| 高效记录 | 单组重量与次数记录的中位时间小于 8 秒 |
| 建立进步反馈 | 同动作按重量、次数、估算力量和训练量判断真实突破 |
| 形成训练节奏 | 自动记录每周训练天数，同日多练仅计一个训练日 |
| 观察身体变化 | 支持体重、体脂和同一动作的力量趋势 |
| 保证可靠性 | 训练过程自动保存，草稿恢复成功率接近 100% |

## 3. 信息架构

底部导航包括“进展”、中心“＋”和“记录”。“进展”是默认首页；中心“＋”拉起动作选择 Sheet，不对应独立页面；“记录”按天展示训练历史。个人档案从进展页右上角进入，不新增“我的”Tab。

核心流程为：进展或记录 → 点击中心加号 → 选择四个动作 → 开始训练 → 按组记录重量和次数 → 完成训练 → 展示真实进步反馈 → 自动打卡 → 返回进展。

身体数据流程为：进展 → 更新体重与体脂 → 保存 → 刷新首页和趋势。草稿恢复流程为：重新打开 App → 检测未完成训练 → 继续训练或放弃。力量趋势流程为：进展 → 力量趋势 → 选择动作 → 查看该动作趋势。

## 4. 首次使用

首次启动简短说明产品价值，并允许设置每周训练目标，默认 3 次且可跳过。随后进入空状态进展首页，用户可直接点击中心加号开始第一次训练。首次启动不强制完成个人档案；用户第一次查看能量消耗时，再引导补齐必要字段。

## 5. 进展首页

进展首页需要快速回答三个问题：最近身体和力量有什么变化、本周已经训练几次、最近训练表现如何。页面依次包含顶部导航、最近趋势、本周训练和近期训练。

最近趋势通过分段控件切换体重、体脂和力量。首次默认体重，之后默认上次查看的指标；时间范围支持最近 4 周、3 个月和 1 年。不同单位不得叠加在同一坐标系。趋势区下方提供“更新体重与体脂”入口。

本周训练展示周一至周日、今日、已完成日期和“本周 2/3 次”等目标进度。完成训练自动点亮当天，同日多次训练仅计算一个训练日。点击有记录的日期进入详情。周目标可在档案中设置为 1–7 次；单日未训练不造成连续记录清零；达成周目标时展示轻量庆祝状态。

近期训练展示最近两次训练，包括日期、星期、训练部位、四个动作、总组数和是否出现重量、次数或估算力量突破。“查看全部”切换至记录 Tab。

无数据时，趋势提示“记录身体数据，开始观察变化”，周日历保持未点亮，近期训练提示“开始你的第一次训练”，并通过中心加号引导开始。

## 6. 动作选择与自定义动作

点击中心加号后拉起原生半浮层。初始为中等高度，搜索或浏览完整动作库时可展开；下滑或点击关闭按钮可取消，关闭后停留在原 Tab。本次 App 会话内暂时保留已选择动作。

顶部分类为“最近使用、胸、背、腿、肩”，支持动作名称和别名搜索。排序优先级为最近使用、常用动作、同部位其他动作、自定义动作。每次训练必须选择四个不重复动作；选满四个后“开始训练”可用；继续选择其他动作时提示先取消一个。动作旁展示上一次有效表现。后续可将四个动作保存为常用组合。

动作库 MVP 至少提供 32 个常见动作，每个部位不少于 8 个。动作数据包括稳定 ID、名称、别名、主要及辅助部位、器械、负重类型、默认次数范围、默认加重档位和是否自定义。

负重类型包括普通负重、自重动作和辅助动作。普通负重以更高重量表示负重增加；自重动作以次数或附加重量判断进步；辅助动作以更低辅助重量或相同辅助重量下更多次数表示进步。

当搜索无结果或动作库缺少目标动作时，必须展示明确的“创建自定义动作”入口，不允许出现空白页面。创建表单包含名称、别名、主要部位、器械、负重类型、默认次数范围和默认加重档位。搜索词自动预填为名称。名称必填且不得与现有动作重复；次数必须为 1–100 的正整数，最低次数不得高于最高次数。创建成功后加入动作库、自动选中，并返回仍然打开的动作选择 Sheet。已选满四个动作时应提示用户先取消一个。

自定义动作被历史记录引用后，即使后续从动作库删除，历史记录仍保留动作名称快照。

## 7. 训练页面与组记录

训练页面展示当前动作名称、动作序号、上一次表现、本次目标、组记录、完成当前动作、查看其他动作和结束训练入口。

每组保存重量、次数和完成状态。默认带入上一次有效训练的组数、重量和次数；无历史数据时按动作默认值创建四组。支持新增和删除组，每个动作至少完成一组才可标记完成。重量支持小数，单位默认为 kg；次数必须为正整数。完成组可再次编辑，编辑后重新计算当前结果。

重量或次数采用点按数值后弹出底部编辑 Sheet 的方式。编辑器支持直接输入、加减按钮、重量加减档位和“同步后续未完成组”。重量范围为 0–500 kg 且最多一位小数，次数范围为 1–100。普通负重、自重和辅助动作应采用匹配各自语义的初始数据和进步判断。

“上一次”必须通过稳定动作 ID 关联，并来自最近一次已完成且至少包含一个有效工作组的训练；草稿、放弃训练和已删除记录不参与。展示上一次最高有效重量及对应次数、各组数据和日期。首次记录显示“首次训练，建立你的基准”。

本次目标采用双进步逻辑：未达到目标次数上限时保持重量并增加目标总次数；主要工作组达到次数上限后建议增加一个最小重量档位；明显未完成时下次保持目标。用户可随时修改或忽略。对外只使用“上次表现”“本次目标”“下次可尝试”等克制表达。

训练过程必须自动保存。App 进入后台、被系统终止、误关闭训练页面或设备短暂重启后，草稿不得丢失。重新打开后提供“继续训练”和“放弃本次训练”，放弃需二次确认。

默认四个动作全部完成后可完成训练。存在未完成动作时仍可结束，但需二次确认；未完成动作标记为跳过，不参与力量趋势。

## 8. 完成反馈与游戏化边界

完成单组时使用不超过约 400ms 的弹性点亮、轻量触感和自然数字过渡，不阻断继续操作。完成训练时使用 SF Symbols、文字缩放、颜色扩散和轻量粒子，持续约 1–2 秒并可立即跳过。

反馈必须展示最具体且真实的结果，例如“卧推突破，62.5 kg × 8，比上次增加 2.5 kg”，或“本周第 3 次训练完成，本周目标已达成”。MVP 可包含周目标点亮、动作重量里程碑、真实突破反馈、完成动效和触感；不包含金币、经验、排行榜、连续签到惩罚、每日强制任务、虚拟宠物或复杂剧情。

## 9. 训练记录

记录页按天展示垂直时间轴，仅显示存在训练或手动打卡的日期。默认倒序，同日多次训练按完成时间倒序，月份作为粘性分组标题。

每条记录展示日期、星期、训练部位、开始时间或时长、四个动作摘要、完成组数、主要突破，以及自动训练或手动打卡标记。详情页展示四个动作及每组重量和次数、与上一次同动作对比、本次突破、是否计入周打卡，并提供编辑和删除入口。

编辑后重新计算动作历史、力量趋势、个人突破和周训练次数。删除前二次确认，删除后提供短暂撤销；若当天没有其他训练或手动打卡，同步取消当天打卡。

手动打卡用于记录 App 外完成的训练，可填写日期、训练部位和可选备注。它计入周训练天数并显示在时间轴，但不生成重量、次数或力量趋势。

## 10. 个人档案、身体数据与能量估算

个人档案包括出生日期、生理性别、身高、当前体重、可选体脂、活动水平、每周训练目标及数据和隐私设置。出生日期用于自动计算年龄。

更新身体数据通过原生 Sheet 完成，字段为日期、体重和可选体脂。可只更新其中一项，同日可修改已有记录；保存后立即刷新首页与趋势。每条记录保存数据来源，MVP 均为手动。历史记录可在对应趋势详情中编辑或删除。

每日能量消耗需区分静息消耗估算和每日总消耗估算。Mifflin–St Jeor 公式为：男性 REE = 10 × 体重kg + 6.25 × 身高cm − 5 × 年龄 + 5；女性 REE = 10 × 体重kg + 6.25 × 身高cm − 5 × 年龄 − 161。每日总消耗估算 = REE × 活动系数。

| 活动水平 | 系数 |
|---|---:|
| 久坐、很少运动 | 1.2 |
| 每周轻度活动 1–3 次 | 1.375 |
| 每周中等活动 3–5 次 | 1.55 |
| 每周高强度活动 6–7 次 | 1.725 |
| 高强度训练或高体力工作 | 1.9 |

界面应表达为“预计每日消耗：约 2,350 kcal。根据你的身体档案和活动水平估算，仅供参考。”必要字段未填写时不显示结果，并提示完善档案。

## 11. 数据趋势与进步判定

体重和体脂趋势展示记录点、最近值、周期变化和平滑趋势线。零条数据时展示更新入口，一条数据仅显示点，两条以上绘制趋势线。体脂必须标注为用户录入值，不暗示医学准确性。

力量趋势先选择动作，再展示动作名称、估算力量趋势、历史最高重量、最近表现和周期变化百分比。普通负重默认展示估算力量；自重动作展示单组最高次数和附加重量变化；辅助动作展示辅助重量变化及相同辅助重量下的次数变化。不同动作不得以公斤数直接合并。

同一动作可判定重量突破、次数突破、估算力量突破和训练量突破。训练量 = Σ 每个有效工作组的重量 × 次数。普通负重动作可使用保守估算 1RM；仅使用 1–10 次有效组，并取本次最高估算结果，只做同一用户同一动作的纵向比较，界面必须标注“估算力量”，不得描述为真实最大力量。

庆祝优先级依次为估算力量突破、重量突破、次数突破、训练量突破、本周目标进展和单纯完成训练。没有真实突破时不得显示“你变强了”，改用“本周第 2 次训练完成”“四个动作全部完成”或“保持训练节奏”。

## 12. 数据模型

| 实体 | 核心字段 |
|---|---|
| UserProfile | 出生日期、生理性别、身高、活动水平、周目标 |
| BodyMeasurement | 日期、体重、体脂、数据来源 |
| Exercise | 稳定 ID、名称、别名、部位、器械、负重类型、次数范围、加重档位、是否自定义 |
| WorkoutTemplate | 名称、四个动作及顺序 |
| WorkoutSession | 开始时间、结束时间、状态、训练部位 |
| ExerciseEntry | 动作 ID、名称快照、顺序、完成状态、历史关联 |
| SetEntry | 重量、次数、完成状态、组序号 |
| CheckIn | 日期、来源、关联训练、训练部位 |

训练状态包括 draft、inProgress、completed 和 abandoned，分别表示已选动作但未开始、训练进行中、训练完成和用户放弃。

## 13. 异常与边界状态

实现必须覆盖动作无历史数据、历史训练仅部分组完成、重复动作、训练中退出、替换或跳过动作、同日多次训练、仅更新体重或体脂、修改或删除历史训练、删除被历史引用的自定义动作、跨时区或午夜前后完成训练、档案不完整、小数重量、自重与辅助动作、减弱动态效果、本地空间不足和保存失败。

## 14. 数据、隐私与技术约束

MVP 本地优先，不要求账号；训练和身体数据默认保存在设备本地，无网络时核心功能完整可用，不上传原始身体和训练数据用于分析。用户可删除全部本地数据，操作需二次确认。后续接入 HealthKit 时按数据类型分别申请权限，仅在用户主动使用同步时申请，拒绝权限不影响手动记录。

原生实现使用 SwiftUI、SwiftData、Swift Charts、原生 Sheet 与 Presentation Detents、SF Symbols 和原生触感反馈。后续可接入 HealthKit 与 UserNotifications。不使用 WebView、跨平台框架或第三方 UI 组件库。

应用支持浅色与深色模式、动态字体、VoiceOver 和至少 44pt 点击区域。开启“减弱动态效果”时，以淡入与颜色变化替代弹性和粒子动效。庆祝动效不得阻塞数据保存和后续操作；新增、编辑和删除历史数据后必须重新计算相关结果。

## 15. 验收标准

| 范围 | 验收条件 |
|---|---|
| 开始训练 | 中心加号打开动作 Sheet；选四个不同动作后才能开始；关闭后停留原 Tab；三段主要操作内进入训练 |
| 训练记录 | 动作支持多组重量和次数；显示最近有效历史；退出后草稿不丢；完成后生成完整记录 |
| 自定义动作 | 无结果状态可创建；字段校验正确；创建后加入动作库、自动选中并返回选择 Sheet |
| 进步反馈 | 仅满足规则时显示突破；无突破时提供完成或周目标反馈；动效可跳过且不阻塞保存 |
| 打卡 | 完成训练自动点亮当天；同日多练只增加一个训练日；手动打卡不生成力量趋势 |
| 数据趋势 | 数据编辑删除后重新计算；力量趋势按动作查看；不同指标不共用坐标轴；数据不足显示正确状态 |
| 档案估算 | 缺失必要字段时不显示误导结果；更新体重后刷新估算；明确标注估算性质 |
| 可访问性 | 浅色、深色、动态字体、VoiceOver、44pt 点击区域和减弱动态效果均可用 |

## 16. 核心产品指标

核心指标包括首次启动后 24 小时内完成第一次训练的比例、点击加号后成功开始训练的比例、训练完成率、使用历史数据记录的比例、首周完成至少两次训练的比例、第二周继续训练的比例和每周身体数据更新率。

体验指标包括点击加号到开始训练的中位时间小于 30 秒、单组记录中位时间小于 8 秒、草稿恢复成功率接近 100%、训练记录保存成功率接近 100%。保护性指标包括训练中途放弃率、动作 Sheet 关闭率、历史编辑后的数据异常率，以及用户关闭动效或触感反馈的比例。
